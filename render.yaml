services:
  - type: web
    name: votera-backend
    runtime: go
    rootDir: backend
    buildCommand: go build -o app .
    startCommand: ./app
    plan: free
    envVars:
      - key: MONGO_URI
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        sync: false